import type {ViewStyle} from "react-native";
import {BrandColor} from "../../structs/brand-color";

export const boxShadow: ViewStyle = {
    shadowColor: BrandColor.MUTED,
    shadowOpacity: 0.2,
    shadowRadius: 5,
};
